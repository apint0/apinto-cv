[project]
name = "apinto-bio"
version = "0.1.0"
description = "Build tooling for adrianopinto's personal CV site (generates index.html from content/ + templates/)."
requires-python = ">=3.9"
dependencies = [
    "jinja2>=3.1",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
